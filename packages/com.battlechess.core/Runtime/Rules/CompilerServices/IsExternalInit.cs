// See the matching file in Contracts. The type must be internal to each
// assembly that uses `init`, so a second copy here is expected, not a mistake.

namespace System.Runtime.CompilerServices
{
    internal static class IsExternalInit
    {
    }
}
